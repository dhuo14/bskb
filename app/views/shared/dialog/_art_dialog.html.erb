<script type="text/javascript">
	var flash = "<%= raw flash[:notice] %>";
	var tips = "<%= raw flash[:tips] %>"
	if(flash.length > 0 ) {
		flash_dialog(flash);
	}
	if(tips.length > 0 ) {
		tips_dialog(tips);
	}
</script>