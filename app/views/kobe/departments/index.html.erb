  <%= render :partial => '/kobe/ztree_show',:locals => { :act => "index", :get_path => ztree_kobe_departments_path, :post_path => move_kobe_departments_path, :current_node_id => @dep.nil? ? current_user.department.id : @dep.id } %>
